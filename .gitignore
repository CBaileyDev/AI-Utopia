# Brainstorming scratch (companion screens, session state)
.superpowers/

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Future implementation artifacts (not yet present; pre-claimed)
.venv/
__pycache__/
*.pyc
*.pyo
node_modules/
target/
build/
dist/
*.egg-info/

# Runtime data (will live under var/lib/aiutopia in production)
var/
runs/
*.ckpt
*.pt

# Secrets (allowlist README + .gitkeep)
secrets/*
!secrets/.gitkeep
!secrets/README.md

# Fabric mod build artifacts
fabric_mod/.gradle/
fabric_mod/build/
fabric_mod/.idea/

# Local server runtime (mods, world, logs — not part of source)
server-runtime/

# Transient research run logs / pids / probe outputs.
# Committed research artifacts are .md / .json (kept) — only the run logs are noise.
Research/*.log
Research/*.log.pid
Research/*.log.session*
Research/*.pid
Research/.probe-*

# Runtime data (created by GUI/spawn; WAL SQLite must stay off the repo per CLAUDE.md)
identity.db
identity.db-*
chroma/
planner_state.db*
config/rewards.json
Research/train-gui-*.pid
